# Python Dependencies for AI News Aggregator
# ==========================================
# Install with: pip install -r requirements.txt

# Supabase Python client
# Used for all database operations
supabase>=2.0.0

# RSS feed parsing
# Handles various RSS/Atom feed formats automatically
feedparser>=6.0.0

# HTTP requests library
# Used for NewsAPI calls and custom fetching
requests>=2.28.0

# Date/time handling (already in stdlib, but useful for complex operations)
python-dateutil>=2.8.0
